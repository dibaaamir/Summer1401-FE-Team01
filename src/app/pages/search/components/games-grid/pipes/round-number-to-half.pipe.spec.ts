import {RoundNumberToHalfPipe} from './round-number-to-half.pipe';

describe('RoundNumberToHalfPipe', () => {
    it('create an instance', () => {
        const pipe = new RoundNumberToHalfPipe();
        expect(pipe).toBeTruthy();
    });
});
