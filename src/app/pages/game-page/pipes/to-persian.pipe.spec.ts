import {ToPersianPipe} from './to-persian.pipe';

describe('ToPersianPipe', () => {
    it('create an instance', () => {
        const pipe = new ToPersianPipe();
        expect(pipe).toBeTruthy();
    });
});
